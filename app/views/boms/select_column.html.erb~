<div class="container" >
<script>
        function change_1(){
               document.getElementById('send_up1').checked = false; 
               document.getElementById('send_up2').checked = false; 
               document.getElementById('send_up4').checked = false;                 
           } 
      
</script>
<%= @bom.excel_file_identifier.inspect %>
    <div class="row ">
    <%= form_tag('/boms', method:"get", class:"form-inline") do %>
        <table class="table table-bordered">
            <tr>
                <% @sheet.row(1).each do |item| %>
                <td>
                    <ul class="list-unstyled">
                    <li>
                     <div class="checkbox-inline">
                        <label>
                            <input type="checkbox" value="<%= item %>" name="select_part[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/><strong>Part</strong>
                         </label>      
                     </div>
                     </li> 
                     <li>
                     <div class="checkbox-inline">
                        <label>
                            <input type="checkbox" value="<%= item %>" name="select_quantity[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/><strong>Quantity</strong>
                         </label>  
                     </div> 
                     </li> 
                     <li>
                     <div class="checkbox-inline">
                        <label>
                            <input type="checkbox" value="<%= item %>" name="select_refDes[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/><strong>RefDes</strong>
                         </label>      
                     </div> 
                     </li> 
                     <li>
                     <div class="checkbox-inline">
                        <label>
                            <input type="checkbox" value="<%= item %>" name="select_description[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/><strong>Description</strong>
                         </label>  
                     </div>
                     </li> 
                    </ul>
                </td>
                <% end %>
            </tr>
            <% @sheet.parse.each do |row| %>
                <tr>
                <% row.each do |item| %>
                    <td width="100" style="word-wrap:break-word;word-break:break-all;">
                        <%= item %>
                    </td>
                <% end %>
                </tr>
            <% end %>
        </table>
        <div class="modal-footer">
            <%= submit_tag "提交", class:"btn btn-primary", :onclick => "return postform()" %>
        </div>
    <% end %>
    </div>




    <div class="row ">
        <%= form_tag('/upload', method:"get", class:"form-inline") do %>
        <table class="table table-bordered">
            <tr>
                <td width="100"><strong>Part</strong></td>
                <td>
                    <% @sheet.row(1).each do |item| %>
                    <div class="checkbox-inline">
                           <label>
                               <input type="checkbox" value="production" name="select_part[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/>
                               <strong><%= item.inspect %></strong>
                           </label>
                     </div>
                     <% end %>
                </td>
            </tr>
            <tr>
                <td width="100"><strong>Quantity</strong></td>
                <td>                    
                    <% @sheet.row(1).each do |item| %>
                    <div class="checkbox-inline">
                           <label>
                               <input type="checkbox" value="production" name="select_quantity[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/>
                               <strong><%= item.inspect %></strong>
                           </label>
                     </div>
                     <% end %>
                </td>
            </tr>
            <tr>
                <td width="100"><strong>RefDes</strong></td>
                <td>                    
                    <% @sheet.row(1).each do |item| %>
                    <div class="checkbox-inline">
                           <label>
                               <input type="checkbox" value="production" name="select_refdes[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/>
                               <strong><%= item.inspect %></strong>
                           </label>
                     </div>
                     <% end %>
                </td>
            </tr>
            <tr>
                <td width="100"><strong>Description</strong></td>
                <td>                    
                    <% @sheet.row(1).each do |item| %>
                    <div class="checkbox-inline">
                           <label>
                               <input type="checkbox" value="production" name="select_description[]" id="receive_feedback_receive_feedback_<%= item.to_s %>" onclick="change_2()"/>
                               <strong><%= item.inspect %></strong>
                           </label>
                     </div>
                     <% end %>
                </td>
            </tr>
        </table>
        <div class="modal-footer">
            <%= submit_tag "提交", class:"btn btn-primary", :onclick => "return postform()" %>
        </div>
        <% end %>
    </div>
</div>
