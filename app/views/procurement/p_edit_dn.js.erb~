document.getElementById('<%= @itemid.to_s + "_" + @dnid.to_s %>').innerHTML='<%= raw @view_dn %>';
$('#editModal').modal('hide')
$(function () 
      { $("[data-toggle='popover']").popover();
      });
