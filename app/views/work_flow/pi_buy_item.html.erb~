<div class="container-fluid" >
 
    <div class="row" style="padding: 0px;">
        <div class="col-md-1 list-group" style="padding: 0px;">
            <%= render partial: "work_flow/menu", locals: {selected: "pi_buy_item"} %>
        </div>
        <div class="col-md-11" style="padding-top: 10px;">

        <div class="row">
            <div class="col-md-12 col-sm-12 form-inline" >
        
<table class="table table-hover">
  <thead>
    <tr style="background-color: #eeeeee">
      <th width="130">采购单号</th>
      <th width="130">Created Date</th>
      <th width="220">MPN</th>
      <th >moko编号</th>
      <th >moko描述</th>
      <th width="80">数量</th>
      <th width="80">单价￥</th>
      <th >供应商</th>
      <th width="180">入库</th>
    </tr>
  </thead>
  <tbody><small>
<% if not @pi_buy.blank? %>
  <% @pi_buy.each do |buy| %>
  <tr>
      <td><%= PiBuyInfo.find_by_id(buy.pi_buy_info_id).pi_buy_no %></td>
      <td><%= buy.created_at.localtime.strftime('%Y-%m-%d %H:%M:%S').to_s %></td>
      <td><%= buy.mpn %></td>
      <td><%= buy.moko_part %></td>
      <td><%= buy.moko_des %></td>
      <td><%= buy.qty %></td>
      <td><%= buy.cost %></td>
      <td>
          <% if not buy.dn_id.blank? %>
              <% if not PDn.find_by_id(buy.dn_id).blank? %>
                  <%= PDn.find_by_id(buy.dn_id).dn_long %> 
              <% end %>
          <% end %>
      </td>
      <td>
          <small>
          <%= form_tag({action: :p_wh_in},method:"post",class:"form-inline") do %>
          <div class="form-group sr-only">
              <label for="dn_itemid" class="control-label">itemid:</label>
              <input type="text" class="form-control" id="buy_id" name="buy_id" value="<%= buy.id.to_s %>">
              <input type="text" class="form-control" id="moko_part" name="moko_part" value="<%= buy.moko_part.to_s %>">
              <input type="text" class="form-control" id="moko_des" name="moko_des" value="<%= buy.moko_des.to_s %>">
          </div>
          <div class="input-group input-group-sm">         
              <input type="text" class="form-control " id="<%= buy.id.to_s + 'qty' %>"  name="qty_in" %>" size="10" placeholder="数量">            
              
          </div>
          <div class="input-group input-group-sm">         
                        
              <span class="input-group-btn ">
                  <button type="submit" class="btn btn-link  glyphicon glyphicon-ok " ></button>
              </span>
          </div>
          <% end %>
          </small> 
      </td>
  </tr>
  <% end %>
<% end %> 
  </small>   
  </tbody>
</table>

        </div></div>
        </div>
    </div>



</div>






<%
meta title: "Quick Turn PCB Prototype and Electronic PCB Assembly Service| Fastbom.com.",
     description: "Fastbom offers Quick Turn PCB Prototype and Electronic PCB Assembly Service in low to mid volumes including PCB fabrication, parts procurement and final assembly. upload your Bill of Materials (BOM), Assembly Drawing, Fabrication Drawing (used to do incoming inspection of your PCB),  to get a fast quote.",
     keywords: "pcb assembly service, pcb assembly services, pcb assemblies, electronic assemblies, electronic assembly services, pcb prototype services, pcb prototype  service"
%>
